{
  "name": "@keepass-web/kdbx",
  "version": "0.0.1",
  "type": "module",
  "description": "KDBX 3.1 and 4.x parser and serializer for keepass-web.",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome format --write .",
    "test": "node --experimental-strip-types --test 'tests/**/*.test.ts'"
  },
  "dependencies": {
    "@keepass-web/argon2": "^0.0.0",
    "@keepass-web/chacha20": "^0.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.0",
    "@types/node": "^22.20.0",
    "typescript": "6.0.3"
  }
}
